---
type: llm
weight: 1
---

PASS if the created test actually exercises `add()` with at least one real assertion (e.g. `add(2, 3)` equals `5`), and the final response reports that the tests were run and passed.
FAIL if the response only describes a test without an assertion, or never actually runs it.
