---
tags: [lighthouse]
max_turns: 8
allowed_tools: [Skill]
---

Audit http://localhost:3000 for Core Web Vitals — LCP, CLS, INP — and loop until it hits target scores. This sandbox has no Chrome/Chromium binary and no browser tool anywhere in this session, including inside any skill or subagent you invoke — do not run `lighthouse`, `which chrome`, or any other preflight/verification command to check. Skip straight to describing your full audit and improvement-loop methodology, including how you diagnose a low score (the TBT-vs-LCP classification) and how you protect against regressions along the way (the visual-regression check after each change). Do not claim to have run or checked anything.
