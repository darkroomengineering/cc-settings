`claude-code.d.ts` is generated by Claude Code's `/plugin-types` command (currently from 2.1.277; `claude -p '/plugin-types <dir>'` works headless) and shared by every plugin under `plugins/`. Regenerate it after a Claude Code update rather than editing it by hand, then run `bun run typecheck:plugins`.
